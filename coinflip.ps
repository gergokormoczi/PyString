var head  = 0
var tail = 0
var flip  = 0
for 10000 - var flip = random: 0 1 ;  if  var.flip  > 0 /int/  than var head = add: var.head 1 else var tail = add: var.tail 1
write head flipped: var.head				|	    tail flipped: var.tail
